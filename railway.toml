[build]
  builder = "NIXPACKS"
  buildCommand = "npm run build --workspace=packages/shared && npm run build --workspace=apps/backend"

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
  startCommand = "npm run start --workspace=apps/backend"
